export function ScoringForm() {
  return (
    <aside
      className="tw:min-h-64 tw:border-t tw:border-neutral-200 tw:bg-neutral-50 tw:p-5 tw:lg:min-h-0 tw:lg:overflow-y-auto tw:lg:border-t-0 tw:lg:border-l"
      aria-labelledby="scoring-form-title"
    >
      <h1 id="scoring-form-title" className="tw:text-lg tw:font-semibold">
        Scoring Form
      </h1>
    </aside>
  );
}
